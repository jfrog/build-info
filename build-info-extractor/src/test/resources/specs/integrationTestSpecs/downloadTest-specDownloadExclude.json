{
  "files": [
    {
      "pattern": "${REPO}/${TEST_SPACE}/a/*",
      "target": "${TEMP_FOLDER}/${TEST_SPACE}/1_pattern_exclude_a/",
      "flat": "true",
      "recursive": "false",
      "excludePatterns": ["${REPO}/${TEST_SPACE}/a/a1.in"]
    },
    {
      "pattern": "${REPO}/${TEST_SPACE}/a/b/*",
      "target": "${TEMP_FOLDER}/${TEST_SPACE}/2_pattern_exclude_b/",
      "flat": "true",
      "recursive": "true",
      "excludePatterns": ["${REPO}/${TEST_SPACE}/*"]
    },
    {
      "pattern": "${REPO}/${TEST_SPACE}/a/b/c/*",
      "target": "${TEMP_FOLDER}/${TEST_SPACE}/3_pattern_exclude_c/",
      "flat": "true",
      "recursive": "true",
      "excludePatterns": ["${REPO}/${TEST_SPACE}/*c1.in", "${REPO}/${TEST_SPACE}/*c2.i?", "${REPO}/${TEST_SPACE}/*ant-antlr-1.6.5.z*"]
    }
  ]
}